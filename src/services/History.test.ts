import { HistoryService } from "./History"

test('loads', () => {
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    let historyService: HistoryService = HistoryService.instance
})
